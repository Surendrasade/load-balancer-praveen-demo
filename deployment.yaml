apiVersion: apps/v1
kind: Deployment
metadata:
  name: cycle-deployment
  namespace: argocd
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cycle
  template:
    metadata:
      labels:
        app: cycle
    spec:
      containers:
      - name: cycle-container
        image: shaikmustafa/dm
        ports:
        - containerPort: 80
